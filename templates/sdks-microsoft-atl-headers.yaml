# install with `cella add sdks/microsoft/atl-headers`

info:
  id: sdks/microsoft/atl-headers
  adopt-vsix-version-from-id: Microsoft.VC.$(SxSVersion).ATL.Headers.base
  summary: Active Template Library Headers

contacts:
  Billy O'Neal III:
    email: bion@microsoft.com
    role: publisher

settings:
  include:
    .

vsixVersionRequire:
  crts/microsoft/headers: Microsoft.VC.$(SxSVersion).CRT.Headers.base

install:
  vsix: Microsoft.VC.$(SxSVersion).ATL.Headers.base
  strip: 7
