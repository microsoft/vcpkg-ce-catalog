# install with `cella add sdks/microsoft/atl`

info:
  id: sdks/microsoft/atl-spectre/arm64
  adopt-vsix-version-from-id: Microsoft.VC.$(SxSVersion).ATL.X86.Spectre.base
  summary: Active Template Library w/ Spectre mitigations
  options: ['dependencyOnly']

contacts:
  Billy O'Neal III:
    email: bion@microsoft.com
    role: publisher

settings:
  libpath:
    .

vsixVersionRequire:
  sdks/microsoft/atl-headers: Microsoft.VC.$(SxSVersion).ATL.Headers.base

install:
- vsix: Microsoft.VC.$(SxSVersion).ATL.ARM64.Spectre.base
  strip: 9
