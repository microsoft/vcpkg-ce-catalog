# OpenOCD
# install with `cella add openocd`

info:
  id: tools/openocd/openocd
  version: 0.11.0
  description: >-
    OpenOCD provides on-chip programming and debugging support with a
    layered architecture of JTAG interface and TAP support including:
    (X)SVF playback to facilitate automated boundary scan and FPGA/CPLD
    programming;
    debug target support (e.g. ARM, MIPS): single-stepping,
    breakpoints/watchpoints, gprof profiling, etc;
    flash chip drivers (e.g. CFI, NAND, internal flash);
    embedded TCL interpreter for easy scripting.
    Several network interfaces are available for interacting with OpenOCD:
    telnet, TCL, and GDB. The GDB server enables OpenOCD to function as a
    "remote target" for source-level debugging of embedded systems using
    the GNU GDB program (and the others who talk GDB protocol, e.g. IDA
    Pro).
  summary: Free and open on-chip debugging

contacts:
  Ben McMorran: 
    email: bemcmorr@microsoft.com
    role: publisher

  OpenOCD:
    email: openocd-user@lists.sourceforge.net
    role: originator

windows and x64:
  install:
    untar: https://github.com/microsoft/openocd/releases/download/ms-v0.11.0/openocd-ms-v0.11.0-i686-w64-mingw32.tar.gz
    sha256: 213e0139ed2a95de5a44ade226c4ed4d7ed3ef4782328aa14b048161ff42aaf6
    strip: 1

windows:
  settings:
    tools:
      openocd: bin/openocd.exe

    paths:
      - bin

linux and x64:
  install:
    untar: https://github.com/microsoft/openocd/releases/download/ms-v0.11.0/openocd-ms-v0.11.0-linux.tar.gz
    sha256: 7751375455913d5ba2b62757dcbec225e3f9dcef48b76079d49dafd01698a761
    strip: 1

not windows:
  settings:
    tools:
      openocd: bin/openocd

    paths:
      - bin
