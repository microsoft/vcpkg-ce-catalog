# Python
# install with `cella add python`

info:
  id: compilers/python
  version: 3.9.5
  description: >-
    Python is a programming language that lets you work more quickly and integrate your systems more effectively.
    You can learn to use Python and see almost immediate gains in productivity and lower maintenance costs.
    
  summary: The Python programming language

contacts:
  Marc Goodner: 
    email: mgoodner@microsoft.com
    role: publisher

windows and x64:
  install:
    unzip: https://www.python.org/ftp/python/3.9.5/python-3.9.5-embed-amd64.zip
    md5: 0b3a4a9ae9d319885eade3ac5aca7d17
    strip: 1 # 

  settings:
    tools:
      # tools exposed by name
      # todo: add more tools to expose by name
      python: python.exe

    paths:
      # we'd prepend these paths to the PATH environment variable. 

windows and x86:
  install:
    unzip: https://www.python.org/ftp/python/3.9.5/python-3.9.5-embed-win32.zip
    md5: cacf28418ae39704743fa790d404e6bb
    strip: 1 # 

  settings:
    tools:
      # tools exposed by name
      # todo: add more tools to expose by name
      python: python.exe

    paths:
      # we'd prepend these paths to the PATH environment variable. 
